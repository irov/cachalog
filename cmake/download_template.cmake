CMAKE_MINIMUM_REQUIRED(VERSION 3.5)

if(CACHALOT_DOWNLOADS_SILENT)
    SET(CACHALOT_GIT_PROGRESS FALSE)
    SET(CACHALOT_DOWNLOAD_NO_PROGRESS 1)
    SET(CACHALOT_FILE_DOWNLOAD_SHOW_PROGRESS FALSE)
else()
    SET(CACHALOT_GIT_PROGRESS TRUE)
    SET(CACHALOT_DOWNLOAD_NO_PROGRESS 0)
    SET(CACHALOT_FILE_DOWNLOAD_SHOW_PROGRESS TRUE)
endif()

MESSAGE("CACHALOT_DOWNLOADS_SILENT: " ${CACHALOT_DOWNLOADS_SILENT})
MESSAGE("CACHALOT_GIT_PROGRESS: " ${CACHALOT_GIT_PROGRESS})
MESSAGE("CACHALOT_DOWNLOAD_NO_PROGRESS: " ${CACHALOT_DOWNLOAD_NO_PROGRESS})
MESSAGE("CACHALOT_FILE_DOWNLOAD_SHOW_PROGRESS: " ${CACHALOT_FILE_DOWNLOAD_SHOW_PROGRESS})

include(ExternalProject)

find_package(Git REQUIRED)

macro(CACHALOT_DOWNLOAD_FILE NAME URL FILE)
    if(NOT EXISTS ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}/${FILE})
        if(CACHALOT_FILE_DOWNLOAD_SHOW_PROGRESS)
            file(DOWNLOAD ${URL} ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}/${FILE} SHOW_PROGRESS)
        else()
            file(DOWNLOAD ${URL} ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}/${FILE})
        endif()
    endif()
endmacro()

macro(CACHALOT_DOWNLOAD_URL NAME URL)
    ExternalProject_Add(${NAME}_download PREFIX ${NAME}
        SOURCE_DIR ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}
        
        URL ${URL}
        DOWNLOAD_NO_PROGRESS ${CACHALOT_DOWNLOAD_NO_PROGRESS}

        CONFIGURE_COMMAND ""
        BUILD_COMMAND ""
        UPDATE_COMMAND ""
        INSTALL_COMMAND ""
    )
    
    add_library(${NAME} STATIC IMPORTED)
endmacro()

macro(CACHALOT_DOWNLOAD_URL_HASH NAME URL HASH_ALGO HASH)
    ExternalProject_Add(${NAME}_download PREFIX ${NAME}
        SOURCE_DIR ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}
        
        URL ${URL}
        URL_HASH ${HASH_ALGO}=${HASH}
        DOWNLOAD_NO_PROGRESS ${CACHALOT_DOWNLOAD_NO_PROGRESS}

        CONFIGURE_COMMAND ""
        BUILD_COMMAND ""
        UPDATE_COMMAND ""
        INSTALL_COMMAND ""
    )
    
    add_library(${NAME} STATIC IMPORTED)
endmacro()

macro(CACHALOT_GIT_CLONE NAME REPOSITORY)
    set(TAG ${ARGN})
    
    list(LENGTH TAG EXIST_TAG)
    
    if(${EXIST_TAG} GREATER 0)
        ExternalProject_Add( "${NAME}_${TAG}_download" PREFIX ${NAME}
            SOURCE_DIR ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}
            
            GIT_REPOSITORY ${REPOSITORY}
            GIT_TAG ${TAG}
            GIT_PROGRESS ${CACHALOT_GIT_PROGRESS}

            CONFIGURE_COMMAND ""
            BUILD_COMMAND ""
            UPDATE_COMMAND ""
            INSTALL_COMMAND ""
        )
    else()
        ExternalProject_Add(${NAME}_download PREFIX ${NAME}
            SOURCE_DIR ${CACHALOT_DEPENDENCIES_SOURCE_DIRECTORY}/${NAME}
            
            GIT_REPOSITORY ${REPOSITORY}
            GIT_PROGRESS ${CACHALOT_GIT_PROGRESS}

            CONFIGURE_COMMAND ""
            BUILD_COMMAND ""
            UPDATE_COMMAND ${GIT_EXECUTABLE} pull
            INSTALL_COMMAND ""
            )
    endif()
    
    add_library(${NAME} STATIC IMPORTED)
endmacro()